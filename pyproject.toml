[project]
name = "ravenfall-webops"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "playwright>=1.57.0",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.40.0",
]
